baseURL = 'https://itchronicles.org/'
languageCode = 'en-us'
title = 'itchronicles'

# Enforce trailing slashes in URLs
uglyURLs = false
canonifyURLs = true

[pagination]
pagerSize = "4"

[params]
  description = "IT blog and knowledge base"
  dateFormat = "2.Jan.2006"

[taxonomies]
  category = "categories"
  author = "authors"

[outputs]
  home = ["HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true
        title = true
        image = true